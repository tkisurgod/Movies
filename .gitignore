# NOTE: config.js is deliberately NOT ignored. It holds only public-by-design
# browser keys (Supabase anon, TMDB) and must ship for GitHub Pages to work.
# See CREDENTIAL-PROTECTION.md before putting anything new in it.

# Dependencies
node_modules/

# TMDB dataset — 44 MB of public Kaggle data, not worth versioning.
# See backend/database/README.md to re-download.
backend/database/*.csv

# Chroma vector store — regenerate with `python database/ingest_data.py`
backend/database/chroma_db/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Environment
.env
.env.local

# Editor / OS
.DS_Store
Thumbs.db

SUPABASE_SETUP.md
REVAMP.md
CREDENTIAL-PROTECTION.md
FUTURE_TODOS.md
